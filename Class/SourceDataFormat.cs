using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using  DBOperation.Class;
using DBOperation.Resource;

namespace DBOperation.Class
{
   public  class SourceDataFormat
    {
       /// <summary>
       /// 绑定的comobox的数据格式转换
       /// </summary>
       /// <param name="recordInfo"></param>
       public static void RecordFormat(RecordInfoBean recordInfo)
       {
       
       }
       /// <summary>
       /// 相机文件预定义 对于未输入的参数取默认值  在文件中存储
       /// </summary>
       /// <param name="camBean"></param>
       public static void CameraFormat(CameraBean camBean)
       {
         
       }
       /// <summary>
       /// 天候文件预定义
       /// </summary>
       /// <param name="whBean"></param>
       public static void Wheatherformat(WeatherBean whBean)
       {

       }

    }
}
